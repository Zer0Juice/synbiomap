# Core data science
pandas>=2.0
numpy>=1.26
scipy>=1.11
statsmodels>=0.14

# HTTP and API clients
requests>=2.31
httpx>=0.25

# Embedding models
# SPECTER2 requires the `adapters` library for its adapter-based architecture.
# adapters pins a compatible transformers version, so we let it manage that.
sentence-transformers>=2.6
adapters>=1.3
torch>=2.0

# Dimensionality reduction and clustering
umap-learn>=0.5
hdbscan>=0.8

# Geocoding
geopy>=2.4

# Environment variable loading
python-dotenv>=1.0

# Quarto-compatible notebook execution
nbformat>=5.9
nbconvert>=7.0
jupyter>=1.0
ipykernel>=6.0

# Reactive .py notebooks (the tripartite analysis is a Marimo app)
marimo>=0.9

# Plotting and interactive visualization
matplotlib>=3.8
plotly>=5.18
altair>=5.0

# Progress bars in notebooks
tqdm>=4.66

# Optional: speed up OpenAlex paging
pyalex>=0.13
